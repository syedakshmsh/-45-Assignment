let username : string[] = ["mariyam", "marwa","muntaha","zunaira","azaan"]
//use for loop foreach method
username.forEach(oneuser => {
    if (oneuser === "mariyam"){
        console.log(`hello ${oneuser} would you like to see a status report?`);
        
    }
})