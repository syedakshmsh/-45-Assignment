var username = ["mariyam", "marwa", "muntaha", "zunaira", "azaan"];
username.forEach(function (oneuser) {
    if (oneuser === "mariyam") {
        console.log("hello ".concat(oneuser, " would you like to see a status report?"));
    }
});
